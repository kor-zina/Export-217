`raw_export_data` contains raw initial data, extracted from a Telegram chat

`export_example` contains public examples of messages.html

`refiner` is a collection of batch ETL jobs for parsing and ML pipelines

`shared` is a collection of SQLAlchemy models, shared enums, Pydantic DTOs, DB utilities, and shared constants

`web-server` is a FastAPI server