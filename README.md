`baseline_export` contains raw initial data, extracted from a Telegram chat

`cleaner` is meant to contain several modules used for processing the data

`clean_export` is meant to contain the processed data